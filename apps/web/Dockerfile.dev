FROM node:18

WORKDIR /app/apps/web

# Install deps during container startup (dev): npm install is run by docker-compose command
EXPOSE 3000

CMD ["sh", "-c", "npm install && npm run dev -- -H 0.0.0.0"]
